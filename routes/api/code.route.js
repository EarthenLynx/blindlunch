// Import needed modules
const express = require("express");
// const {handleCreateCode, handleGetCode, handleGetCodeById, handleDeleteCodeById, handleUpdateCodeById} = require("../../controller/code.controller");

// Setup the router
var router = express.Router();

// router.get("/", (req, res, next) => {
//   handleGetCode(req, res, next);
// })

// router.get('/:id', (req, res, next) => {
//   handleGetCodeById(req, res, next)
// })

// router.post("/", (req, res, next) => {
//   handleCreateCode(req, res, next);
// })

// router.delete("/:id", (req, res, next) => {
//   handleDeleteCodeById(req, res, next);
// })

// router.put("/:id", (req, res, next) => {
//   handleUpdateCodeById(req, res, next);
// })

module.exports = router;
